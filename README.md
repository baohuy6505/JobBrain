# JobBrain - Hệ thống Tuyển dụng Thông minh
## 📂 Cấu trúc dự án
- `/frontend`: Chứa mã nguồn giao diện (Vite + ReactJS + Tailwind CSS v4).
- `/backend`: Chứa mã nguồn máy chủ API (NodeJS + Express).
- DB MongoDB Altas (Clound): 
- Cấu hình sử dụng tại .env : file ni sẽ được gửi riêng trong nhóm 
+ Cấu hình môi trường được lưu trong file `.env`.
+ File `.env` **không được commit**, sẽ được chia sẻ riêng trong nhóm.
---

## 🛠 Hướng dẫn cài đặt cho thành viên nhóm

### 1. Yêu cầu hệ thống
- Đã cài đặt [Node.js](https://nodejs.org/) (Phiên bản 18 trở lên).
- Trình quản lý gói `npm`.

### 2. Cài đặt chi tiết
Sau khi clone project về máy, cần cài đặt thư viện cho cả 2 phần:

**Cho Frontend:**
Terminal 1:
```bash
cd frontend
npm install

**Cho Backend:**
Terminal 2:
```bash
cd backend
npm install

**Chạy dự án**
npm run dev 
